<%= form_with(model: messaging_service) do |form| %>
  <% if messaging_service.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(messaging_service.errors.count, "error") %> prohibited this messaging_service from being saved:</h2>

      <ul>
        <% messaging_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :alias_name, class: "label" %>
    <%= form.text_field :alias_name, class: "input" %>
  </div>

  <div class="field">
    <%= form.label :provider, class: "label" %>
    <%= form.select :provider, MessagingService.providers.keys, class: "input" %>
  </div>

  <div class="field">
    <%= form.label :webhook_url, class: "label" %>
    <%= form.text_field :webhook_url, class: "input" %>
  </div>

  <div class="field">
    <%= form.submit class: "button is-primary" %>
  </div>
<% end %>
